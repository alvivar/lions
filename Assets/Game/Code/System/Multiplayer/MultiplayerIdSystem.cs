using System.Collections.Generic;
using UnityEngine;

// #jam
public class MultiplayerIdSystem : MonoBehaviour
{
    public static List<MultiplayerId> components = new List<MultiplayerId>();

    private void Update()
    {
        foreach (var c in components)
        {

        }
    }
}